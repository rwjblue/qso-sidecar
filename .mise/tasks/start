#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; -*-
#MISE description="Start the release QSO Sidecar binary"

set -euo pipefail
if [[ ! -x target/release/qso-sidecar ]]; then
  cargo build --release
fi
exec target/release/qso-sidecar "$@"

