#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; -*-
#MISE description="Verify formatting, Clippy, and tests"

set -euo pipefail
cargo fmt --all --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-features

